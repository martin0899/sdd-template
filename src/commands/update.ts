export async function runUpdate(): Promise<number> {
  console.log('spectralis update is not available yet (TypeScript port in progress, phase 2).');
  console.log('Canonical fallback: run install.sh --update from a clone of the template repository.');
  return 0;
}
